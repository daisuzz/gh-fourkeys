# gh-inspect

```shell
$ export GITHUB_API_TOKEN=<Your GitHub API token>
$ go run main.go -o <Repo Owner> -r <Repo name> -b <Base branch name>
Merged PR count within 6 months: 100

Average time to merge: 1h00m00s

Top 10 changed files within 6 months:
sample.go: 10
sample2.go: 2
```
